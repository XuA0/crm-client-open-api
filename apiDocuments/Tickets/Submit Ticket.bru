meta {
  name: Submit Ticket
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/private/v1/ticket/submit
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "subject": "string",
    "content": "string",
    "atts": [
      {
        "name": "string",
        "url": "string",
        "mimetype": "string"
      },
      {
        "name": "string",
        "url": "string",
        "mimetype": "string"
      }
    ]
  }
}

settings {
  encodeUrl: true
}

example {
  name: OK
  
  request: {
    url: {{BASE_URL}}/private/v1/ticket/submit
    method: POST
    mode: json
    headers: {
      Content-Type: application/json
    }
  
    body:json: {
      {
        "subject": "string",
        "content": "string",
        "atts": [
          {
            "name": "string",
            "url": "string",
            "mimetype": "string"
          },
          {
            "name": "string",
            "url": "string",
            "mimetype": "string"
          }
        ]
      }
    }
  }
  
  response: {
    status: {
      code: OK
      text: 200
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
