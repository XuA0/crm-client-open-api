meta {
  name: POST /private/v1/mtr/accounts/:id(\\d+)/password/reset/demo
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}/private/v1/mtr/accounts/:id(\\d+)/password/reset/demo
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:json {
  {
    "connection_id": ""
  }
}

settings {
  encodeUrl: true
}
